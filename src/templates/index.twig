{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * Czechitoviny plugin for Craft CMS 3.x
 *
 * Czechitoviny index.twig
 *
 * @author    Lucie Charvat
 * @copyright Copyright (c) 2018 Lucie Charvat
 * @link      https://github.com/lucidvoci
 * @package   Czechitoviny
 * @since     1.0.0
 */
#}

{% extends "_layouts/cp" %}
{% import "_includes/forms" as forms %}

{% do view.registerAssetBundle("czechitas\\czechitoviny\\assetbundles\\czechitoviny\\CzechitovinyAsset") %}
{% do view.registerAssetBundle("czechitas\\czechitoviny\\assetbundles\\indexcpsection\\IndexCPSectionAsset") %}

{# Link for the ? icon at the bottom of the page #}
{% set docsUrl = "https://github.com/czechitoviny/czechitoviny/blob/master/README.md" %}

{# The title of this CP section #}
{% set title = "Czechitoviny" %}

{# The URL to this plugin's base CP section #}
{% set pluginCpUrl = url('czechitoviny') %}

{# Get a URL to an image in our AssetBundle #}
{% set iconUrl = view.getAssetManager().getPublishedUrl('@czechitas/czechitoviny/assetbundles/indexcpsection/dist', true) ~ '/img/Index-icon.svg' %}

{# Content that should appear in the page header#}
{% set extraPageHeaderHtml %}
    <div class="buttons">
        <a href="{{ pluginCpUrl }}" class="btn submit add icon">{{ "Click Me!"|t('czechitoviny/tmp') }}</a>
    </div>
{% endset %}

{% set fullPageForm = true %}

{% block actionButton %}
    {# <input type="submit" class="btn submit" value="{{ 'Save'|t('user') }}"> #}
{% endblock %}

{# The content of the CP Section #}
{% set content %}
    {% if users %}
    <table class="user-table">
        <thead>
            <tr>
                <th> Jméno </th>
                <th> Příjmení </th>
                <th> Datum narození </th>
                <th> E-mail </th>
                <th> Pohlaví </th>
                <th> Město </th>
                <th> Telefon </th>
                <th> Role </th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
            <tr>
                <td> {{ user.firstName }} </td>
                <td> {{ user.lastName }} </td>
                <td> {{ user.birth }} </td>
                <td> {{ user.email }} </td>
                <td> {{ user.gender }} </td>
                <td> {{ user.city }} </td>
                <td> {{ user.phone }} </td>
                <td> {{ user.role }} </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    {% endif %}

    {# <input type="hidden" name="action" value="czechitoviny/default/save-user">
    {{ redirectInput('czechitoviny') }}
    {{ csrfInput() }}



    {{ forms.textField({
        first: true,
        label: "First Name",
        instructions: "---",
        id: 'firstName',
        name: 'firstName'
    }) }}
    {{ forms.textField({
        first: true,
        label: "Second Name",
        instructions: "---",
        id: 'secondName',
        name: 'secondName'
    }) }}
    <h2>{{ "Index CP Section body goes here"|t('czechitoviny') }}</h2>
    <img src="{{ iconUrl }}" height="64" width="64" />
    <p class="textline"></p> #}
{% endset %}
